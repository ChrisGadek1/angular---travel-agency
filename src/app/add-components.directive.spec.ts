import { AddComponentsDirective } from './add-components.directive';

describe('AddComponentsDirective', () => {
  it('should create an instance', () => {
    const directive = new AddComponentsDirective();
    expect(directive).toBeTruthy();
  });
});
