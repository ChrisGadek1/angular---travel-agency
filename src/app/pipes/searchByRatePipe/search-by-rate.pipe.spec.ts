import { SearchByRatePipe } from './search-by-rate.pipe';

describe('SearchByRatePipe', () => {
  it('create an instance', () => {
    const pipe = new SearchByRatePipe();
    expect(pipe).toBeTruthy();
  });
});
