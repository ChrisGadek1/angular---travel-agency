import { CanSeeWithoutPlacesPipe } from './can-see-without-places.pipe';

describe('CanSeeWithoutPlacesPipe', () => {
  it('create an instance', () => {
    const pipe = new CanSeeWithoutPlacesPipe();
    expect(pipe).toBeTruthy();
  });
});
