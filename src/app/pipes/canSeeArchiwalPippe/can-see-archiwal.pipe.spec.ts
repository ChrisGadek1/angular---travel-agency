import { CanSeeArchiwalPipe } from './can-see-archiwal.pipe';

describe('CanSeeArchiwalPipe', () => {
  it('create an instance', () => {
    const pipe = new CanSeeArchiwalPipe();
    expect(pipe).toBeTruthy();
  });
});
